const body = document.querySelector('body');
const themeText = document.querySelector('.themeSwitch__wrapper');


const toggleTheme = () => {
  
};

themeSwitcher.addEventListener('click', toggleTheme);